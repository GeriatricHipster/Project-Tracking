<label> Building <div className="select-with-button"> <select value={form.location} onChange={(event) => { const next = event.target.value; if (next === '__custom__') { addCustomBuilding(); return; } updateField('location', next); }}> <option value="">Unassigned</option> {buildingChoices.map((building) => ( <option key={building} value={building}>{building}</option> ))} <option value="__custom__">Add new building...</option> </select> <button className="ghost-button compact" type="button" onClick={addCustomBuilding}>Add</button> </div> </label>
